% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dou_em_mstep.R
\name{dou_em_mstep}
\alias{dou_em_mstep}
\title{M-step for Bernstein Copula EM Algorithm (Dou et al.)}
\usage{
dou_em_mstep(tau_bar, maxiter = 1000, tol = 1e-10)
}
\arguments{
\item{tau_bar}{A numeric matrix of pseudo-counts (typically from E-step).}

\item{maxiter}{Integer. Maximum number of iterations. Default is 1000.}

\item{tol}{Numeric. Convergence tolerance. Default is 1e-10.}
}
\value{
A normalized matrix \code{R} such that \code{sum(R) = 1} and
        \code{R[i,j] ≈ tau_bar[i,j] / (mu[i] + lambda[j])}.
}
\description{
Performs the M-step of the EM algorithm described in Dou et al. (2016),
estimating the Bernstein copula weight matrix by solving a constrained
optimization problem via iterative bisection.
}
\examples{
set.seed(1)
tau <- matrix(runif(16), 4, 4)
R <- dou_em_mstep(tau)
sum(R)  # should be approximately 1
}
