% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_pdf.R
\name{bernstein_joint_pdf_grid}
\alias{bernstein_joint_pdf_grid}
\title{Joint PDF Grid from Bernstein Copula and MarginalDistributions}
\usage{
bernstein_joint_pdf_grid(x, y, R, Fx, Gy)
}
\arguments{
\item{x}{Numeric vector. Grid of x values.}

\item{y}{Numeric vector. Grid of y values.}

\item{R}{Matrix of size m x n. Bernstein copula weight matrix.}

\item{Fx}{A \code{MarginalDistribution} object for variable X.}

\item{Gy}{A \code{MarginalDistribution} object for variable Y.}
}
\value{
A matrix of joint density values \code{h(x, y)} with dimensions \code{length(x) × length(y)}.
}
\description{
Evaluates the joint density \eqn{h(x, y)} over a grid of values using a Bernstein copula and two \code{MarginalDistribution} objects.
}
\examples{
x <- seq(-2, 2, length.out = 50)
y <- seq(-2, 2, length.out = 50)
Fx <- normaldist(mean = 0, sd = 1)
Gy <- normaldist(mean = 0, sd = 1)
R <- matrix(1/4, 2, 2)
Z <- bernstein_joint_pdf_grid(x, y, R, Fx, Gy)
plot_contour(x, y, Z)
}
