% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sinkhorn.R
\name{sinkhorn_scaling}
\alias{sinkhorn_scaling}
\title{Sinkhorn Scaling for Bernstein Copula Matrix}
\usage{
sinkhorn_scaling(W, maxiter = 1000, tol = 1e-10)
}
\arguments{
\item{W}{A numeric matrix with non-negative entries.}

\item{maxiter}{Integer. Maximum number of iterations. Default is 1000.}

\item{tol}{Numeric. Convergence tolerance. Default is 1e-10.}
}
\value{
A scaled matrix of the same dimension as \code{W}, normalized to meet the marginal constraints.
}
\description{
Scales a non-negative matrix \code{W} so that its row and column sums match the Bernstein copula normalization constraints.
After convergence, the scaled matrix will have row sums equal to \code{1 / nrow(W)} and column sums equal to \code{1 / ncol(W)}.
}
\examples{
W <- matrix(runif(16), 4, 4)
R <- sinkhorn_scaling(W)
round(rowSums(R), 6)  # Should be close to rep(1/4, 4)
round(colSums(R), 6)  # Should be close to rep(1/4, 4)
}
